import { features } from "../constants";
import styles, { layout } from "../style";
import Button from "./Button";

const FeatureCard = ({ icon, title, content, index }) => (
  <div className={`flex flex-row p-6 rounded-[20px] ${index !== features.length - 1 ? "mb-6" : "mb-0"} feature-card`}>
    <div className={`w-[64px] h-[64px] rounded-full ${styles.flexCenter} bg-dimBlue`}>
      <img src={icon} alt="star" className="w-[50%] h-[50%] object-contain" />
    </div>
    <div className="flex-1 flex flex-col ml-3">
      <h4 className="font-poppins font-semibold text-white text-[18px] leading-[23.4px] mb-1">
        {title}
      </h4>
      <p className="font-poppins font-normal text-dimWhite text-[16px] leading-[24px]">
        {content}
      </p>
    </div>
  </div>
);

const Business = () =>  (
  <section id="features" className={layout.section}>
    <div className={layout.sectionInfo}>
      <h2 className={styles.heading2}>
        Experience the social side of crypto,  <br className="sm:block hidden" /> secure your deals.
      </h2>
      <p className={`${styles.paragraph} max-w-[470px] mt-5`}>
      With Cryptogram, you can now connect with cryptocurrency wallet addresses. This opens up a world of possibilities for users, allowing them to discover and engage with fellow crypto enthusiasts such as P2E gamers or DEX traders.
      Socializing is not everything. With Cryptogram, users can also securely sign contracts with others and rest assured that they'll be enforced fairly by impartial judges selected at random from CGM holders.
      Join the Cryptogram community today, it's time to take control of your crypto world.
      </p>

      <Button styles={`mt-10`} />
    </div>

    <div className={`${layout.sectionImg} flex-col`}>
      {features.map((feature, index) => (
        <FeatureCard key={feature.id} {...feature} index={index} />
      ))}
    </div>
  </section>
);

export default Business;